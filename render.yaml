services:
  - type: worker
    name: ai-motivation-worker
    runtime: python
    repo: https://github.com/YOUR_USERNAME/ai-motivation-uploader
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    autoDeploy: true
